# Base URL the frontend uses to reach the API.
# Only relevant for `npm run dev` (no Docker, no reverse proxy in front) - point this at
# wherever your API is actually running.
VITE_API_BASE_URL=http://localhost:5067/api
